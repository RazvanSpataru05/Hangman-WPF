<Window x:Class="Hangman.Game.OpenSaveWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hangman.Game"
        mc:Ignorable="d"
        Title="Open Save" Height="350" Width="450"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        FontFamily="Ink Free">
    <Window.Background>
        <ImageBrush ImageSource="pack://application:,,,/Assets/background.jpg"
                   Stretch="UniformToFill"
                   Opacity="0.9"/>
    </Window.Background>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select a save to load"
                   FontSize="14" FontWeight="SemiBold" Margin="0 0 0 8"/>
        <ListBox Grid.Row="1" ItemsSource="{Binding Saves}"
                 SelectedItem="{Binding SelectedSave}"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Category" FontSize="10" Foreground="Gray"/>
                            <TextBlock Text="{Binding Category}" FontSize="12" FontWeight="SemiBold"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Level" FontSize="10" Foreground="Gray"/>
                            <TextBlock Text="{Binding CurrentLevel}" FontSize="12" FontWeight="SemiBold"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Mistakes" FontSize="10" Foreground="Gray"/>
                            <TextBlock Text="{Binding Mistakes}" FontSize="12" FontWeight="SemiBold"/>
                        </StackPanel>

                        <StackPanel Grid.Column="3">
                            <TextBlock Text="Time Left" FontSize="10" Foreground="Gray"/>
                            <TextBlock Text="{Binding TimeLeft}" FontSize="12" FontWeight="SemiBold"/>
                        </StackPanel>

                        <StackPanel Grid.Column="4">
                            <TextBlock Text="Save Date" FontSize="10" Foreground="Gray"/>
                            <TextBlock Text="{Binding SaveTime}" FontSize="12" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="2" Margin="0 10 0 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Cancel" Width="80" HorizontalAlignment="Center"
            Command="{Binding CancelCommand}"
            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
            <Button Grid.Column="1" Content="Load" Width="80" HorizontalAlignment="Center"
            Command="{Binding LoadCommand}"
            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
        </Grid>
    </Grid>
</Window>
