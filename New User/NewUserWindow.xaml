<Window x:Class="Hangman.NewUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Hangman.Helper"
        Title="New User" Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        FontFamily="Ink Free">
    <Window.Resources>
        <local:StringToImageConverter x:Key="StringToImage"/>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Ink Free"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center">
                        </ContentPresenter>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.1" ScaleY="1.1"></ScaleTransform>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Window.Background>
        <ImageBrush ImageSource="pack://application:,,,/Assets/background.jpg"
                   Stretch="UniformToFill"
                   Opacity="0.9"/>
    </Window.Background>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0"
                   Text="Enter Username" FontWeight="SemiBold" 
                   FontSize="14"
                   Margin="0 20 10 5"/>

        <TextBox Grid.Row="1" Grid.Column="0"
                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,10,0"/>

        <StackPanel Grid.Row="2" Grid.Column="0"
            Grid.ColumnSpan="2"
            Orientation="Vertical"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <Image Source="{Binding ImageSelector.CurrentImage, Converter={StaticResource StringToImage}}"
           Stretch="Uniform"
           MaxHeight="150"
           MaxWidth="150"/>
            <StackPanel Orientation="Horizontal"
                HorizontalAlignment="Center"
                Margin="0,10,0,0">
                <Button Content="&lt;&lt;" Style="{StaticResource ButtonStyle}"
                        Command="{Binding ImageSelector.PreviousImageCommand}" Width="25" Margin="25 0" Height="25"/>
                <Button Content="&gt;&gt;" Style="{StaticResource ButtonStyle}"
                        Command="{Binding ImageSelector.NextImageCommand}" Width="25" Margin="25 0" Height="25"/>
            </StackPanel>
        </StackPanel>

        <Button Grid.Row="4" Grid.Column="0"
                Content="Cancel"
                Command="{Binding CancelCommand}"
                Style="{StaticResource ButtonStyle}"
                FontSize="18"
                FontWeight="SemiBold"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                HorizontalAlignment="Left" Width="80"/>

        <Button Grid.Row="4" Grid.Column="1"
                Content="Add User"
                Command="{Binding AddUserCommand}"
                Style="{StaticResource ButtonStyle}"
                FontSize="18"
                FontWeight="SemiBold"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                HorizontalAlignment="Right" Width="80"/>
    </Grid>
</Window>